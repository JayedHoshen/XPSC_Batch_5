#include <bits/stdc++.h>
#define FIO() ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define fi0(n) for(int i = 0; i < n; i++)
#define ll long long int
using namespace std;

int main()
{
    FIO(); 

    string s; cin >> s;

    if(s == "WECNITK") cout << "Welcome to Web Club!" << '\n';
    else cout << "Access denied" << '\n';

    return 0;
}